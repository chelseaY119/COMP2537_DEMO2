<%- include("templates/header") %>
<div style="margin: 2%; height: 74vh">
    <% if (pageTitle ==='403' ) { %>
        <h2>Sorry, this site cannot be accessed - ERROR 403 </h2>
        <% } %>

            <% if (pageTitle ==='404' ) { %>
                <h2>Page not found - 404 </h2>
                <% } %>

                    <br> <br>
                </div>
                    <%- include("templates/footer") %>