<%- include("templates/header") %>

    <!-- users list -->
    <ul>
        <% users.forEach((user)=> { %>
            <br>
            <%- include('templates/user', { 
                user: user, 
                user_type: 'user',
                buttonText1: 'Promote to Admin' , 
                buttonText2: 'Demote to User'
                }) %>
                <% }) %>
    </ul>
 
    <%- include("templates/footer") %>