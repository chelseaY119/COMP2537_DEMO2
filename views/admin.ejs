
<%- include("templates/header") %>
<div style="height: 74vh; margin: 3%">

    <div style="margin: 2%">
        <h2>Users</h2>
    </div>
  
<% admins.forEach((admin)=> { %>
    <br>
    <%- include('templates/user', { user: admin, buttonText1: 'Promote to Admin' , buttonText2: 'Demote to User' }) 
        %>
        <% }) %>
        
    <% users.forEach((user)=> { %>
        <br>
        <%- include('templates/user', { user: user, buttonText1: 'Promote to Admin' , buttonText2: 'Demote to User' })
            %>
            <% }) %>

        </div>
<%- include("templates/footer") %>
